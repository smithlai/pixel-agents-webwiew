# GooseOffice — 本地環境設定範例
# 複製此檔為 .env.local（已加入 .gitignore，不會上傳）並填入實際路徑

# Goose JSONL 事件監視目錄
# 指向 Goose session 輸出資料夾，例如 MobileGoose 的 .runtime/sessions
# 支援相對路徑（相對於 webview-ui/）或絕對路徑
#
# 範例：
#   GOOSE_WATCH_DIR=../../MobileGoose/.runtime/sessions
#   GOOSE_WATCH_DIR=C:/Users/你的帳號/MobileGoose/.runtime/sessions
#
# 未設定時：Goose 事件串流停用，自動 fallback 到 browserMock 模擬模式
GOOSE_WATCH_DIR=
